<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dash">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <application
        android:name=".ui.RedditApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="net.dean.jraw.android.REDDIT_USERNAME"
            android:value="geruth" />
        <meta-data
            android:name="net.dean.jraw.android.CLIENT_ID"
            android:value="oXrVL7dWkiIQiw" />
        <meta-data
            android:name="net.dean.jraw.android.REDIRECT_URL"
            android:value="https://www.google.com/" />

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.register.RegisterActivity"
            android:noHistory="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:noHistory="true" />
        <activity
            android:name=".ui.account.AccountActivity"
            android:noHistory="true" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:noHistory="true" />
        <activity android:name=".ui.account.AddRedditAccount" />

    </application>

</manifest>